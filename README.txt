To test that your account is set up correctly, type:

$ make

This will run a script that submits a small test job that should not fail,
thereby testing for configuration issues.

It will report the outcome of the test and email for help if it fails.
